FROM node:22-slim AS build

WORKDIR /app

ARG VITE_VPN_URL
ENV VITE_VPN_URL=${VITE_VPN_URL}

COPY frontend/package*.json ./
RUN npm ci

COPY frontend/ ./
RUN npm run build

FROM caddy:alpine
COPY --from=build /app/dist /srv
COPY docker/Caddyfile /etc/caddy/Caddyfile

EXPOSE 80 443
